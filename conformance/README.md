# AFS Conformance Suite

The conformance suite defines repositories and ADR documents that every AFS implementation should evaluate identically.

`valid/` contains inputs that MUST pass.

`invalid/` contains inputs that MUST fail.

`expected/` contains the expected rule identifiers for each test case.
