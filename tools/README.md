# Tools

This directory contains project tooling.

The current Python package under `src/afs_cli` is the initial implementation of the AFS CLI.
It may later move into `tools/afs-cli` when the repository becomes a multi-package project.
